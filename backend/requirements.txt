fastapi
uvicorn[standard]
sqlalchemy>=2.0
pydantic>=2.10
pydantic-settings>=2.7
httpx
python-multipart
# PostgreSQL driver (optional - only needed for PostgreSQL prod)
# Install separately for production: pip install psycopg2-binary

# Testing
pytest>=8.0
pytest-asyncio>=0.24
pytest-cov>=6.0
